import * as immutable from "immutable";

declare module "immutable" {
    // export interface Iterable<K, V> {
    //     get<VU extends V | undefined>(key: K, notSetValue: VU): V | VU;
    // }

    // export interface Indexed<T> {
    //     get<TU extends T | undefined>(index: number, notSetValue: TU): T | TU;
    // }

    // export interface Seq<K, V> {
    //     get<VU extends V | undefined>(key: K, notSetValue: VU): V | VU;
    // }
}